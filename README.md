# zig_test

HTTP server exposing `/v1/ping` that responds with `pong`.

## Development

- `make build` – compile the server
- `make test` – run the test suite
- `make fmt` – format Zig sources
- `make lint` – check formatting
